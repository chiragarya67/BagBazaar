
    <%- include('./partials/header') %>

<% if(success.length > 0){ %>
  <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
    <span class="inline-block mt-1 mb-1 text-white"><%= success %></span>
  </div>
<% } %>

<div class="w-full h-screen flex items-start px-20 py-20">

  <!-- Sidebar -->
  <div class="w-[25%] h-screen flex flex-col items-start ">
    <div class="flex items-center gap-2">
      <h3>sort by</h3>
      <form action="/shop">
        <select class="border-[1px] px-2 py-1" name="sortby">
          <option value="popular">Popular</option>
          <option value="newest">Newest</option>
        </select>
      </form>
    </div>

    <div class="flex flex-col mt-20">
      <a class="block w-fit mb-2" href="">New Collection</a>
      <a class="block w-fit mb-2" href="">All Products</a>
      <a class="block w-fit mb-2" href="">Discounted Products</a>
    </div>

    <div class="mt-32">
      <a class="block w-fit mb-2" href="">Filter by :</a>
      <a class="block w-fit mb-2" href="">Availability</a>
      <a class="block w-fit mb-2" href="">Discount</a>
    </div>
   <% if (user && user._id.toString() === '683f0a1c33e4ee1d3e575646') { %>
  <form action="/admin/adminpanel" method="POST">
    <button type="submit"
      class="inline-flex items-center gap-2 text-black font-semibold py-3 rounded-lg">
      <i class="ri-add-line text-xl"></i>
      <span>Add Product</span>
    </button>
  </form>
<% } %>


  </div>
  <!-- Product Grid -->
  <div class="w-[75%] flex flex-col gap-5 h-screen">
    <div class="flex flex-wrap items-start gap-5">
      <% products.forEach(function(product){ %>
        <div class="w-60 rounded-xl shadow hover:shadow-lg transition-all overflow-hidden">
          <div class="w-full h-52 flex items-center justify-center bg-[<%= product.bgcolor.trim() %>]">   
            <img class="h-[12rem]" src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="product image">
          </div> 
          <div class="flex justify-between bg-[<%= product.panelcolor.trim() %>] items-center px-4 py-4 text-[<%= product.textcolor.trim() %>]">
            <div>
              <h3><%= product.name %></h3>
              <h4>₹ <%= product.price %></h4>
            </div>
            <a class="w-7 h-7 flex items-center justify-center rounded-full bg-white" href="/addtocart/<%= product.id %>">
              <i class="ri-add-line"></i>
            </a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</div>

<%- include('./partials/footer') %>



 